# Foreground food cutouts

Supply transparent PNG promotional-specials artwork only. Every eligible PNG is discovered; natural filename order determines its presentation sequence. Zero images leaves the welcome overlay clean, one renders strongly, and larger collections scale without requiring a fixed count. README and non-PNG files are ignored.
