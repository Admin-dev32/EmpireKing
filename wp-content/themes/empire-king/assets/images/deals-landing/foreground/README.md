# Foreground food cutouts

Supply transparent PNG product cutouts only. Every eligible PNG is discovered; natural filename order determines its presentation sequence. Zero images leaves the hero clean, one renders strongly, and larger collections wrap and scale without requiring a fixed count. README and non-PNG files are ignored.
