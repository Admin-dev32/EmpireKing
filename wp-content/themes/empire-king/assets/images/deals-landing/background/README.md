# Background slot

Accepted formats: WebP, JPG, JPEG, PNG. Supply one optimized, high-resolution restaurant or food-environment image with a center/cover-safe composition and no baked-in text.

Zero eligible files uses the CSS burgundy fallback. If more than one eligible image is present, the first natural filename order is used deterministically. README and other non-image files are ignored.
